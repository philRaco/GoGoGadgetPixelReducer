/// @description Pause Created

//ah here we go once again

if noPauseRoom() == true{
    cantPause = 1;
} else {
    cantPause = 0;
}

global.pauseState = 0;
bgFondo = -1;

arbiTimer = 3;

reMap = 0;